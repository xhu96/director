import { DirectorCommand } from "@director.run/utilities/cli/director-command";

export function registerDebugCommands(_program: DirectorCommand) {
  // Debug commands are no longer needed in multi-tenant mode
}
