import { whiteBold } from "@director.run/utilities/cli/colors";

export function title(text: string) {
  return whiteBold(`${text.toUpperCase()}`);
}

export function subtitle(text: string) {
  return whiteBold(`${text.toUpperCase()}`);
}
